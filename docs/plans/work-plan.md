# 工作计划表

要求：

1. 工作前在表格里写明工作名称
2. 工作内容 200字以内
3. 状态：（初步规划，已有方案，待开始，进行中，已完成）
4. 工作指导文件，工作范围：前端还是后端，其他补充 1000字以内
5. 工作后，变更状态，记录完成时间
6. 其他说明：细节文档不要补充在这里，另起文档，这个只用于记录工作计划。只能在表格里添加内容，不允许添加其他无意义的内容

| 工作名称 | 工作内容 | 工作指导文件 | 状态 | 计划时间 | 完成时间 | 其他 | 建议 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 前端视觉架构改造 - 东方水墨风格 | 对现有前端视觉架构进行全面改造，采用东方水墨风格设计语言。包括：重新设计配色系统（浅色背景+墨色点缀）、引入书法字体、添加水墨纹理背景、实现毛笔书写动画效果、优化整体视觉层次和交互细节。目标是打造具有中国传统文化特色的现代Web应用界面。 | `docs/standards/visual.md`（详细改造文档） | 已完成 | 2026-07-03 | 2026-07-03 | 改造范围包括：全局CSS变量系统重构，首页视觉升级，书籍工作台视觉升级，AI配置页面视觉升级，组件库样式统一，响应式设计优化，动画和微交互效果 | 添加宣纸纹理背景图片，实现毛笔书写动画效果，添加水墨山水装饰元素，优化移动端响应式体验，添加更多微交互动画 |
| AI配置页 - 配置存储后端化 | 将AI配置页面的厂商配置（config）等内容从前端存储方式改为后端存储。前端只负责展示和获取配置内容，后端负责配置的持久化存储。 | `server/ai/ai-config-store.ts`（后端存储）`app/api/ai/config/route.ts`（API路由） | 已完成 | 待定 | 2026-07-03 | 采用JSON文件存储方案，配置保存在data/ai-config.json，前端通过API获取和保存配置 | 已完成 |
| 书籍创作页 - 前端视觉改造 | 对workspace页面书籍信息表单进行视觉改造：1)优化表单间距，Form.Item间距从24px降至12px，Divider间距同步缩减；2)重构滚动逻辑，滚动条移至页面最右侧，表单主体区域独立滚动；3)保存按钮固定在页面底部可视区域，无需滚动即可点击。 | `app/pages/books/components/book-info-form/index.tsx`（组件）`app/pages/books/components/book-info-form/index.module.css`（样式） | 已完成 | 2026-07-03 | 2026-07-03 | 范围：仅书籍创作页面，不涉及首页。布局重构为flex column三层结构（标题固定/表单滚动/按钮固定），响应式适配768px和480px | 已完成 |
| 故事创作功能 - 创作区与正文库 | 改造"内容区域"为创作区+新增正文库Active。采用滚动规划型：模糊总纲→当前卷纲（详细）→章纲（滚动3-5章）→正文。创作区两栏布局：左侧导航栏（总纲+卷折叠树形章纲列表），右侧8种视图切换（空状态引导/卷纲内容卡片/章纲内容卡片/卷纲编辑表单/章纲编辑表单/正文编辑器/总纲编辑/正文库）。章纲字段丰富：情节概要、场景设定、出场人物、重要情节、伏笔设置、预计看点、字数等。正文库存稿备份独立存储。 | `docs/plans/creation-zone.md`（设计与实施计划） | 已有方案 | 待定 | | 视觉规范：遵循东方水墨配色（globals.css CSS变量）+ antd组件库 + @ant-design/icons。先搭无AI版手动操作框架，后接AI辅助。范围：前端为主。 | 设计稿已确认，待编写实现计划 |
| AI生成公用组件 | 所有需要AI的功能共用一套生成组件。流程：用户输入区→提示词预览区（程序自动组装，用户可查看编辑）→确认按钮→AI生成中→结果展示区（用户审核、可修改、可重新生成）→确认通过进入下一步。组件可定制输入字段以适配不同业务（卷纲/章纲/正文/总纲生成等）。 | 待讨论 | 已有方案 | 待定 | | 公用组件，所有AI生成功能复用。可定制输入字段。无AI对话功能，仅走"输入→预览Prompt→生成→审核"流程。范围：前端组件+后端组装逻辑。 | 配合工作流引擎使用 |
| 工作流引擎 | 支持多节点AI调用的工作流。示例（生成正文）：节点1(AI提取设定)→节点2(程序查找设定库)→节点3(AI整合总结)→节点4(AI生成正文)。每个AI节点有独立Prompt，节点类型分AI节点和程序节点。最终用户可自定义设计工作流（节点、顺序、Prompt）。初期内置固定工作流，后期支持用户自定义。 | 待讨论 | 初步规划 | 待定 | | 多节点串联，节点输出=下节点输入。AI节点调用模型，程序节点做查找/组装。初期固定工作流，后期用户自定义。范围：后端为主。 | 初期固定工作流，后期开放自定义 |
